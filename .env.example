# Quizly environment configuration

# LLM provider selection: 'ollama' or 'openai'
LLM_PROVIDER=ollama

# Ollama settings
OLLAMA_MODEL=llama3.2

# OpenAI settings
OPENAI_API_KEY=your-openai-api-key
OPENAI_MODEL=gpt-3.5-turbo

# Prompt template used for AI question generation
PROMPT_TEMPLATE=Generate {limit} multiple-choice quiz questions about {subject}. Each question should have exactly 4 answer options labeled a, b, c, d. Format your response as a JSON array with fields "text", "options", "correct_answer", and "category". Return only the JSON array.
